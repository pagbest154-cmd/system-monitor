#!/bin/sh
set -e

. /usr/share/debconf/confmodule

db_capb backup

db_input medium system-monitor-agent/hub-url || true
db_input medium system-monitor-agent/agent-id || true
db_input medium system-monitor-agent/token || true

db_go || true
