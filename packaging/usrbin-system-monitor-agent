#!/bin/sh
VENV_PYTHON="/opt/system-monitor-agent/venv/bin/python3"
if [ ! -x "$VENV_PYTHON" ]; then
    echo "system-monitor-agent: runtime not installed. Run: sudo dpkg-reconfigure system-monitor-agent" >&2
    exit 1
fi
exec "$VENV_PYTHON" -m system_monitor.agent "$@"
