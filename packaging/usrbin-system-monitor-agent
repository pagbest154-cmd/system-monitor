#!/bin/sh
AGENT_BIN="/opt/system-monitor-agent/system-monitor-agent"
if [ ! -x "$AGENT_BIN" ]; then
    echo "system-monitor-agent: binary not found at $AGENT_BIN" >&2
    exit 1
fi
exec "$AGENT_BIN" "$@"
